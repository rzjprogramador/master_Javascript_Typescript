import { userResolvers } from '../../modules/user/User.entity.resolvers'

const resolvers = [userResolvers]

// console.log(resolvers) // VER SE ESTA CHEGANDO OS [RESOLVERS]
// fs.readFileSync(path.resolve(__dirname, '', '', '', '*.resolvers')

export default resolvers
